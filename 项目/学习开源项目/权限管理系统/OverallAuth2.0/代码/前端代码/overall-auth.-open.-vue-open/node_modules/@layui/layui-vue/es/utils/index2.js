function e(e2) {
  const { indent: n, level: t, basePadding: i = 0, isTree: r } = e2, o = t - 1;
  if (r && n && o > 0) {
    return `padding-left: ${"boolean" == typeof n ? `${i + 10 * o}px` : n.replace(/\d+/g, (e3) => (i + o * +e3).toString())}`;
  }
  return "";
}
export { e as i };
